{% extends 'base.html.twig' %}

{% block title %}Klasse{% endblock %}

{% block body %}
    <h1>Klasse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ klasse.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ klasse.name }}</td>
            </tr>
            <tr>
                <th>Jahrgang</th>
                <td>{{ klasse.jahrgang }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('klasse_index') }}">back to list</a>

    <a href="{{ path('klasse_edit', {'id': klasse.id}) }}">edit</a>

    {{ include('klasse/_delete_form.html.twig') }}
{% endblock %}